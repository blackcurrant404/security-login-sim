# SECURITY LOGIN SIMULATOR

Simple login simulator that can be used for testing purposes. Program uses hardcoded password that user can attempt to guess. Produces report based on attempts (output + log file)